name = "agentflow"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
ENVIRONMENT = "production"
# SECURITY: set via `wrangler secret put JWT_SECRET` / dashboard — never commit real secrets.
# JWT_SECRET = "CHANGE_ME_via_wrangler_secret"

[[d1_databases]]
binding = "DB"
database_name = "agentflow-db"
database_id = "01cf525e-59ad-4264-b04a-13c698af5b2e"
