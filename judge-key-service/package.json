{
  "name": "agentflow-judge-key-service",
  "private": true,
  "version": "1.0.0",
  "description": "Standalone Cloudflare Pages — judge self-serve temp API key (SHA-256) for AgentFlow",
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name agentflow-judge-key",
    "dev": "wrangler pages dev public --port 8788"
  }
}
